{
  "name": "script",
  "version": "0.0.1",
  "scripts": {
    "build": "cd client && yarn build && cd ../server && yarn build",
    "start-client": "cd client && yarn start",
    "start-server": "cd server && yarn start",
    "pack-mac": "yarn build && cd server && yarn pack-mac",
    "pack-win": "yarn build && cd server && yarn pack-win"
  }
}
