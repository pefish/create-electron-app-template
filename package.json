{
  "name": "script",
  "version": "0.0.1",
  "private": true,
  "productName": "template",
  "scripts": {
    "build": "cd client && yarn build  && rm -rf ../server/build && mv ./build ../server/build && cd ../server && yarn build",
    "start-client": "cd client && yarn start",
    "start-server": "cd server && yarn start",
    "pack-mac": "yarn build && cd server && yarn pack-mac"
  }
}
